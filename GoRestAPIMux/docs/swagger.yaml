info:
  contact: {}
paths:
  /articles:
    get:
      description: Get All Articles
      produces:
      - application/json
      responses:
        "200":
          description: ok
          schema:
            type: string
      summary: Get All Articles
swagger: "2.0"
